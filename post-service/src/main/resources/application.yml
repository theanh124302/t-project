spring:
  application:
    name: post-service
  datasource:
    url: jdbc:postgresql://localhost:5432/t-db
    username: t-user
    password: t-password
  jpa:
    generate-ddl: true
    show-sql: false  # Change this to false
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
cloud:
  aws:
    credentials:
    region:
      static: ap-southeast-1
    bucket:
      name: t-p-bucket
    cloudfront:
      domain: https://d20fdb851jr426.cloudfront.net